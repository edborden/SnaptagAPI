require 'test_helper'

class WebTest < ActiveSupport::TestCase

	#test "web can list current givers and receivers" do
	#	assert @user0.targets.include?(@user1)
	#	assert @user1.hunters.include?(@user0)
	#	hunt = Hunt.find_by(target_id: @user1.id)
	#	hunt.active = false
	#	hunt.save
	#	assert_not @user1.hunters.include?(@user0)
	#end

	test "plus_one and minus_one" do
	end

	test "matching_hunt" do
	end

end